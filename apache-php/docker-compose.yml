web:
  image: bingofresh/apache-php
  ports:
   - "8081:80"
  volumes:
   - ./app:/app
#   - ./logs/apache:/var/log/apache2
  environment:
   MYSQL_MASTER_HOST: 
   MYSQL_SLAVE_HOST:
   MYSQL_MASTER_PORT:
   MYSQL_SLAVE_PORT:
   MYSQL_DBNAME:
   MYSQL_USERNAME:
   MYSQL_PASSWORD:
